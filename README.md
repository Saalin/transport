The solution to one of the tasks from the Computer Networks course at the University of Wrocław, 2019.

Program downloads binary file from the UDP server, which serves file in chunks with a probability of both not sending it or sending more than once. Because of the memory limit it uses a sliding windows algorithm.
